package thanhnt.structural.adapter;

// LegacyService
public class LegacyRectangle {
    public void draw(int x, int y, int w, int h) {
        System.out.format("LegacyRectangle: %d, %d, %d, %d", x, y, w, h);
    }
}
